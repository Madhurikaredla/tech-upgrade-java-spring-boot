# tech-upgrade-java-spring-boot
tech-upgrade-java-spring-boot

## API Documentation (Swagger UI)

After starting the application, access the Swagger UI at:

	http://localhost:5894/swagger-ui.html

## Build & Run Commands

To clean, compile, and run the application using Maven:

```sh
# Clean previous builds
mvn clean

# Compile the project
mvn compile

# Run the Spring Boot application
mvn spring-boot:run
```